# Kong Gateway DB-less 宣言的設定
# API Gateway: 8000 -> app:8080
_format_version: "3.0"
_transform: true

services:
  - name: playjava-api
    url: http://app:8080
    routes:
      - name: playjava-route
        paths:
          - /
        strip_path: false
